# VoxNow Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env files to version control

# =============================================================================
# SUPABASE CONFIGURATION (REQUIRED)
# =============================================================================
# Your Supabase project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anonymous/public key
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# =============================================================================
# ADMIN AUTHENTICATION (REQUIRED)
# =============================================================================
# Temporary admin password - MUST be changed to proper authentication system
# This is a temporary security measure and should be replaced with proper RBAC
VITE_ADMIN_PASSWORD=your-secure-admin-password-here

# =============================================================================
# THIRD-PARTY INTEGRATIONS (OPTIONAL)
# =============================================================================
# OpenAI API Key for voicemail analysis (used in Edge Functions)
OPENAI_API_KEY=your-openai-api-key-here

# Make.com webhook URLs - SECURITY: These are exposed to client-side
# In production, consider moving to server-side proxy endpoints
VITE_MAKE_WEBHOOK_URL=your-make-webhook-url-here
VITE_MAKE_WEBHOOK_TRIAL_URL=your-make-webhook-trial-url-here
VITE_MAKE_WEBHOOK_PT_URL=your-make-webhook-pt-url-here

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. All VITE_ prefixed variables are exposed to the client-side code
# 2. Never put sensitive secrets in VITE_ variables in production
# 3. Use server-side environment variables for sensitive data
# 4. Rotate all keys regularly
# 5. Use different values for development, staging, and production